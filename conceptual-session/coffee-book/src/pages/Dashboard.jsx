import { Helmet } from "react-helmet-async";

const Dashboard = () => {
    return (
        <div>
            <Helmet>
                <title>Dashboard</title>
            </Helmet>
            <h1>Dashboard</h1>
        </div>
    );
};

export default Dashboard;